# 第四组收尾第六轮：无分配诊断对照与 helper 身份漏报修复

更新：2026-09-26。接手 main：`ea5383a87eec94b440c0f63d44334ef81b6e841a`。本轮使用本机 WebCodex Local；继续资源稳定性，不开发导出、不修改 CI、系统时间或旧冻结标签。继承的三个状态文件和第二／三／四轮草稿已单独备份，不混入本轮提交；无关目录保持原样。

## 1. 产品身份与对照条件

没有改变产品 Rust、Swift、Metal、helper 或着色器。沿用并重新核对上一轮 Release：主程序 `548f6903480512136e3d0c34eb185ad47840670a6c7824be8a0d2d8c30bf8330`，helper `f74c4f402bf005eee852db83f272de35c512d43bffb5e868c84da32ca769f68e`，shader `b5f1563b90031cdf588fd00166136fbdcda9dba94df5751324142f46c7b9bbb7`。本轮不将复用产物说成重新构建。

`artifacts/group4-local-20260926-06/source-lock.json` 记录基线、原测试入口哈希、产物哈希及继承改动。测试机是 macOS 27／arm64；桌面解锁，事件、辅助功能与截图权限通过。仍不是 macOS 26 实机验收。

首先运行与第五轮相同的4工作文档、1保活窗口、请求600秒、两段70秒空闲、显式浅色场景；不插入 vmmap、leaks、heap 或分配栈工具。保留原来的只读资源计数和物理足迹采样。期间为核对进程漏报，另做了有限的进程列表／内核路径查询，没有附加到分配器。

## 2. 原入口对照结果及限制

`control-600-light` 原脚本退出码0；完成28个全遍历轮次，实际压力613.727秒，总计801.431秒。源码、原历史、保存、同进程重开及冷重开公式检查完成，固定公式182个对比像素、实际浅色。

| 同类应用阶段 | 第五轮有 vmmap | 本轮未插入分配诊断 |
| --- | --- | --- |
| 保留文档／历史空闲起点（字节） | 237,503,736 | 229,934,280 |
| 同阶段70秒后（字节） | 257,606,880 | 223,888,560 |
| 阶段变化（字节） | +20,103,144 | -6,045,720 |
| 关闭工作文档后空闲末（字节） | 79,873,152 | 69,665,896 |

本轮没有重现此前的20.1MB空闲增长。只有一对非同时、非随机化的有限运行，轮次和系统负载并不完全一致，不能据此断言增长必由 vmmap 引起，也不能宣布无泄漏。应用足迹数据仍有效；下节发现的 helper 观测缺陷使原入口的 helper 计数、helper 足迹和清理断言不能作为验收证据。原始 `passed=true` 不修改，报告明确将这些维度标记为待复验，而不是把旧结果继续算作全场景签收。

## 3. 实际发现的 helper 漏报

生产代码把 `current_exe()` 的父目录拼接 `../Helpers/yu-document-renderer` 交给进程创建。系统实际命令显示为 `Contents/MacOS/../Helpers/yu-document-renderer`；旧 `process_rows` 仅匹配 `Contents/Helpers/yu-document-renderer` 的字符串前缀。

`process-identity-observation.json` 保存了同次进程列表：应用2097、4个真实 helper（2179、2229、2289、2339），旧解析器却返回 `helpers=[]`。第五轮四文档压力的365个内存采样也全为空 helper 列表。因此第五轮三套多文档入口关于 helper 数量、物理足迹、空闲退出及无残留的断言需要复验；这不抹掉对应源码、历史、窗口像素或应用物理足迹证据，也不自动否定使用不同进程观察器的冷重开／主题专项。

本轮原入口结束后，还使用新的内核探针独立核对其两个确切二进制路径，未发现残留。这个终点观察不能补造过程中漏掉的 helper 内存样本。

## 4. 观测链路修复

新增 `tools/process-inventory.c`，通过 libproc 的可执行文件路径和 BSD 进程身份匹配两个确切隔离二进制，不再使用命令行、argv0、basename 或任意字符串包含关系。目录中的空格、中文、换行、软链接和 `..` 不影响真实可执行文件身份。只查询当前用户，输出匹配进程的 PID、父PID、进程组和出生时间，不输出其他应用的命令行或路径。

同用户的部分系统管理进程不允许读取路径；探针单独报告这类无关不可读项数量。隔离组内进程不可读会失败，而不是伪造成 helper 为0；完整性受限的枚举和坏 JSON 同样拒绝。结果描述的是本轮隔离进程，不声称普查全系统。

多文档入口改为使用该探针，确认所有匹配进程仍在本轮创建的进程组内。有效资源帧及冷重开必须有一次实际非空 helper 观察，防止“永远返回空列表”的观察器继续通过。helper 物理足迹前后复核出生身份，正常退出或 PID 被替换时记录缺测状态，既不采纳别的进程内存，也不填成0；同一存活 helper 采样失败则仍失败。源码、撤销历史、缓存容量和原像素门禁均不变。

## 5. 本机回归

先添加真实 C 子进程回归，调用实际 `NativeSoak.processes()`：修改前7项中1项失败，修改后通过。测试使用私有可执行文件，不创建产品窗口，也不终止用户进程；覆盖真实相对 helper 路径、Unicode／空白路径、软链接、同名其他程序、伪造 argv0、退出及非法参数。

加入身份 JSON、正向 helper 门禁和采样期间退出／PID更替回归后，当前72项工具／原生进程测试通过，其中7项需要 macOS libproc。它们不计为72项产品实窗，也不重报上一轮 Rust／Swift 测试次数。C 探针由测试以 `-Wall -Wextra -Werror` 构建。

## 6. 修正入口后的实窗状态

修正后的 `kernel-short-light` 已完整结束，退出码0：2个工作文档、4个全遍历轮次，循环39.484秒、总计211.040秒。请求30秒但完整执行已开始的轮次，没有截短一轮。内核观察实际出现最多2个 helper，已取得真实 helper 物理足迹样本（本次范围1,425,792至6,504,856字节）；两段空闲、原历史验证、同进程重开、冷重开像素及正常退出全部完成，最终内核计数无隔离进程残留。固定公式182个对比像素，背景实测浅色。后续长压力只使用这个已修正并绑定哈希的入口，旧入口不用于声称 helper 内存已经覆盖。

四文档30分钟验证将单独记录完整结果；不会预先标记通过。

第四组仍未结项；无分配诊断对照缩小了归因范围，但最终分配栈归因、多主题长时压力、真实跨午夜／多日睡眠／时区事件和完整选区／长页组合仍按实际缺口跟踪。
