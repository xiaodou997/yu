# macOS IME Spike：2026-08-09

## 环境

- macOS / AppKit
- 自定义 `NSView`
- `NSTextInputClient`
- Swift Package experiment

## 已观察事件

实际启动 spike 后，中文拼音输入产生连续 `setMarkedText`，选词后产生一次 `insertText`：

```text
setMarkedText preedit="y"
setMarkedText preedit="yi"
setMarkedText preedit="yin wen"
insertText commit="引文"
```

另一次输入成功提交 `日语`。emoji 候选提交 `😊` 时，AppKit selection 前进 2 个 UTF-16 code
units，符合系统 `NSRange` 的坐标语义。

## 发现的问题

Escape 取消 composition 时，系统发送 `cancel:` command。第一版 spike 没有处理该命令，而且
为了显示 preedit 已经临时替换了 TextKit storage，因此无法可靠恢复原选择内容。

修正后通过 LaunchServices `.app` 重新实测：输入 `yu yan` 时 preedit 正常显示下划线，Space
提交为 `语言`；随后输入 `shi` 并按 Escape，preedit 被完整移除，正文仍为 `语言`。测试结束后
恢复了此前的系统输入源。

## 架构结论

Rust core 不应在 `setMarkedText` 时修改 `TextBuffer`。正确协议为：

```text
setMarkedText ──► CompositionOverlay update ──► projection/layout only
insertText    ──► one Transaction             ──► TextBuffer
cancel        ──► drop overlay                ──► zero Transaction
```

Swift spike 暂时保存 composition 开始前的 attributed substring，用于验证 Escape 回滚；产品
实现不会依赖这份备份，因为 canonical source 从未被 preedit 修改。

## 验证状态

- [x] 中文拼音连续 preedit 与单次 commit；
- [x] emoji commit 的 UTF-16 长度；
- [x] Escape 回滚；
- [x] `firstRect` 多行 marked preedit 只锚定首个视觉 fragment，AX range frame 保留完整 union；
- [ ] candidate window 是否在真实输入源下持续跟随多行 caret；
- [ ] 日文、dead key、组合重音；
- [x] 日文、组合重音与 cancel 的 `NSTextInputClient` 协议回放；
- [ ] 切换真实日文输入源、dead key 后的人工事件验证；
- [x] 系统 AX 对 UTF-16 text range 与 screen bounds 的查询；
- [x] Yu View 的 AX text entry tree 可被系统 Computer Use 读取；
- [x] composition 期间 AX value、selected range、marked styling、geometry、commit/cancel
  mirror 的协议级 self-check；
- [ ] VoiceOver 对 marked range 的实际朗读；
- [x] Swift UTF-16 range 与 Rust `CompositionOverlay` 的 FFI 转换（最小 C ABI session；详见
  [CompositionOverlay FFI 实验](macos-composition-ffi-2026-08-10.md)）。

启动日志还会记录只读的 `Keyboard input source probe`。该记录用于人工验收时确认当前输入源，
不代表程序已经自动验证了日文输入源或 dead key；两者仍需要切换输入源后的真实按键测试。

交互输入会输出结构化 `IME_EVENT {json}` 行，记录事件顺序、类型、UTF-16 ranges、Revision、
composition generation 和 candidate screen rect。它只记录协议事件，不写入 Markdown source，也不
把人工测试误标为自动通过。

日志可通过 spike 的无窗口 `--audit-ime-log PATH` 模式复核：它检查序号连续、replacement range
稳定、generation 单调、composition 期间 canonical Revision 不变，以及 commit/cancel 是否消费同一
native range/generation。实时窗口被 Ctrl-C 截断时，最后半行只标记为 `truncatedTail=true`；中间的
坏 JSON 仍会使审计失败。日文与组合重音的最小可重复 fixture 位于
`experiments/macos-text-input/fixtures/japanese-combining-ime.log`。
