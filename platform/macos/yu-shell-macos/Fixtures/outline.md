# Yu 大纲

顶层标题下面的一段正文。

## 层级一

### 层级一之下

内容。

#### 更深一级

内容。

## 带 **行内标记** 的标题

第三刀把行内标记剥掉了：面板上是「带 行内标记 的标题」。

## 带 [链接](https://example.com) 的标题

链接的方括号与目标也是被藏起来的 source，面板上只剩「带 链接 的标题」。

## 收尾串 ##

ATX 的收尾 `#` 串由语法树说了算，正文是「收尾串」。

多行
标题
===

Setext 的 label 里有换行，面板上折成一行。

> # 容器里的标题

已登记的欠账：容器里的标题不是 `BlockKind::Heading`，进不了大纲。

### 越级回来

最后一段。
