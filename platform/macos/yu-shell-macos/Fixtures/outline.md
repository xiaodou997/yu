# Yu 大纲

顶层标题下面的一段正文。

```rust
fn 高亮() { let x = 1; }
```

上面这个代码块是给 `--launch-window-self-check` 用的：它要在**真实提交的
那一帧**里数出带高亮颜色的字形。放在首屏之内，否则那一步会以「一个都没有」
假红。大纲不收代码块，所以它不影响这个 fixture 原本的层级断言。

## 层级一

### 层级一之下

内容。

#### 更深一级

内容。

## 带 **行内标记** 的标题

第三刀把行内标记剥掉了：面板上是「带 行内标记 的标题」。

## 带 [链接](https://example.com) 的标题

链接的方括号与目标也是被藏起来的 source，面板上只剩「带 链接 的标题」。

## 收尾串 ##

ATX 的收尾 `#` 串由语法树说了算，正文是「收尾串」。

多行
标题
===

Setext 的 label 里有换行，面板上折成一行。

> # 容器里的标题

已登记的欠账：容器里的标题不是 `BlockKind::Heading`，进不了大纲。

### 越级回来

最后一段。
