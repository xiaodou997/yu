# Render code

```rust
fn main() {
    // 注释
    let x: u32 = 1;
    println!("{x}");
}
```

```json
{ "a": [1, true, null] }
```

```
fn main() {
    // 注释
    let x: u32 = 1;
    println!("{x}");
}
```

没有语言名的那一块与第一块正文完全相同，是这一条 self-check 的对照组。
