# Code baseline calibration

ASCII only:

```
plain ascii
plain ascii
plain ascii
```

CJK on the middle line:

```
plain ascii
中文注释
plain ascii
```

Emoji on the middle line:

```
plain ascii
emoji 👨‍👩‍👧‍👦
plain ascii
```

Swift syntax colors:

```swift
let greeting = "Hello"
print(greeting)
plain(greeting)
```

YU_END_OF_CODE_BASELINE_CALIBRATION
