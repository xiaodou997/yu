# Render image plan

This block keeps the render-plan fill and glyph assertions active. Inline image:
![Yu logo](assets/yu-logo.png)

中文粗体 paragraph keeps the IME surface assertions active.

```rust
fn main() {}
```
